# AdBlock ML Project 
- Decision Pipeline: standardBlocked = staticDNR_only; mlOnlyBlocked = ML AND NOT list. 
- Risk Engine: Track entropy, late injection, and ML hits. 
- Compliance: Manifest V3 strict. No eval(). 
